@{
  Layout = "_Layout";
}

<div class="bg-panel">
  <h1>Activities List</h1>

  <ul class="no-bullet">
    @foreach (var activity in Model)
    {
      <li>@activity.GetName()</li>
    }
  </ul>
</div>
